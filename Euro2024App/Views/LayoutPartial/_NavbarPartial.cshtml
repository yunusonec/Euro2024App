<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand mx-auto d-none d-lg-block" href="@Url.Action("Index", "Default")">EURO2024</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Team")">Takımlar</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Maçlar</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        İstatistikler
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="#">Takım İstatistiği</a></li>
                        <li><a class="dropdown-item" href="#">Oyuncu İstatistiği</a></li>
                        <li><a class="dropdown-item" href="#">Tümü</a></li>
                    </ul>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                @if (User.Identity.IsAuthenticated)
                {
                    <li class="nav-item">
                        <a class="nav-link" href="#">@User.Identity.Name</a>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("SignIn", "Login")">Giriş Yap</a>
                    </li>
                }
            </ul>
        </div>
        <a class="navbar-brand d-lg-none" href="#">Navbar</a>
    </div>
</nav>